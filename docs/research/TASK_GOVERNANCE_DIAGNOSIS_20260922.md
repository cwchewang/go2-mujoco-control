# Governance repair and first-capture offline diagnosis

Parent: `9f7ee6eb226cbdedad5b514328b8fa72463c1d0f`.
Mode: infrastructure plus offline exploratory interpretation.
User requested implementation of the governance audit and explanation of the
first flat policy's poor tracking. No new physical integration is authorized.

Repair automatic preflight classification and runner diffs, bind qualification
to real inputs, reuse applicable offline tests while retaining fresh dynamic
preflight and exact-head authorization, move branch metadata into tracked task
configuration, and verify the external campaign ledger by default locally.
Unify governance scope, generated handoff/archive indexes and inventory policy.
Preserve historical raw, protocol, outcome and campaign claims byte-for-byte.

For diagnosis, pin upstream deployment/configuration/model source to the existing
lock; compare recorded observations/actions/PD with an independent implementation,
inspect effective named model parameters and rebuild contacts with `mj_forward`
under the zero-step guard. Policy inference on recorded inputs is allowed;
new state trajectories, tuning, replacement runs and new capability claims are not.
Command counterfactuals on the fixed recorded input sequence must be labelled
open-loop sensitivity, never predicted closed-loop performance.

Acceptance: targeted regression checks, full clean qualification, exact-head
science/execution review, zero-step guards, unchanged historical evidence,
remote CI, matching clean local/remote checkout and durable diagnostic report.
Result: `docs/validation/governance_diagnosis_20260922/RESULTS.md`.
