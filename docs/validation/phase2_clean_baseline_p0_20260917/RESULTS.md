# Phase2 clean baseline P0 closeout

Date: 2026-09-17
Task commit: `38e340a703ba0397c083d94fcbdc16a2a488728a`
Exact base: `21633a76b8cc9c9e820c2399fb0bdbc4b864260a`
Primary classification: `CLEAN_BASELINE_P0_READY`

All four required invariants are implemented in the opt-in
`--clean-baseline` path. The default and historical paths retain their
existing clamp, fallback, and post-QP overlay behavior.

- Swing WBC acceleration now uses `J*qdd + Jdot*qdot`; focused coverage passes.
- Clean WBC requires the constrained solver acceptance result and safe-holds on failure.
- Clean LowCmd torque is the accepted ID-WBC torque through only the explicit final ramp/absolute safety envelope.
- Clean target-to-IK uses direct IK plus the authoritative Go2/MuJoCo joint ranges; unreachable and out-of-range targets are rejected without projection.

Validation passed:

- 29/29 available portable CTest cases, including rigid-body-independent SRBD MPC, dense QP, clean-baseline, IK, and CLI-route checks.
- MuJoCo-backed `test_go2_rigid_body` and `test_inverse_dynamics_wbc` compiled and passed against the read-only reference MuJoCo installation; the current worktree has no local MuJoCo headers/library, so those targets were not generated by CMake.
- Modified controller translation units passed `-fsyntax-only` with the reference MuJoCo headers.
- `git diff --check` passed.

No live simulator run, performance claim, raw capture, or `_runs` modification was made.
