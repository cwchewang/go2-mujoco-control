# 项目基础与交接方式整理

范围：治理与文档；父提交 fd91d4613d7bd970946840344cfae8af1edcf064。
没有新增物理实验、策略选型或调参。首轮 FAIL 与原证据保持不变。

## 实际修复

OPERATING_GUIDE 为人工及任意模型提供接手、任务分级、决策依据、偏差处理和
交接方法。TASK_TEMPLATE 提供可删减的最小任务记录，实验模板补充参数依据、
原条件复现/迁移区分及 outcome-to-decision。普通编辑复用已有任务/PR，不增加
一套表格、自动代理或逐步审批。用户授权不因换执行者失效。

AGENTS/README/文档入口指向该指南；SOP 明确科学审查要检查问题和参数的依据，
文档任务只做相关检查。治理页移除了旧 Phase 2 路线和“禁止 RL 接入”的冲突，
说明共享平台与外部训练框架边界。PROJECT_RECORD 清除过期 host/下一步提示，
保留历史证据；当前阶段改为用户指定的基础建设，长期 Gate 路线不再等同待执行清单。
已接入权重标为工程候选，0.15 m/s 保留历史意义，不升级为全局任务要求。

## 文档走查：执行者不依赖本次聊天

| 情境 | 路径 | 应得结论 |
|---|---|---|
| 新接手者看到旧 first-capture 命令 | CURRENT → 当前任务 → 指南 → 冻结协议 | 当前范围是基础建设；旧 campaign 已关闭，命令不是新授权 |
| 接到常规文档/实现修复 | 指南的按影响分级 → CONTRIBUTING → 任务完成依据 | 在已授权范围自主修复；文档不跑物理验收，运行改动做相应回归 |
| 被要求选策略/参数，或结果与预期不符 | 指南的决策依据/偏差处理 → 短任务/实验模板 | 默认方案仍为候选；说明参数依据和结果如何影响决策；保留失败，不换条件追 PASS |

这是本次执行者进行的静态桌面走查，不是独立用户测试或 Luna 实测。文档和
工具能减少遗漏，不能替代科学判断。以后出现真实接手困难时修正对应入口或
任务，不以增加无关审批和文档数量作为默认修复。

## 验证与接手

验收使用现有 tools.check_quality --style、workspace --check、git diff --check，
并核对父提交以来仅维护文档和 current.json 变化。具体输出保存在
`_runs/project_foundations_20260923/acceptance_01/`；PR 和最终 main 核验记录绑定
实际 SHA/状态。未重复完整 native qualification；本轮没有运行代码或环境变化。

此后先根据用户指定的下一项工作填写目的、范围、依据和完成条件；当前无新的
科研执行任务，不默认恢复 RL 原条件复现或其他实验。新的科学结果仍按原 SOP
形成证据，本轮没有升级任何能力结论。
