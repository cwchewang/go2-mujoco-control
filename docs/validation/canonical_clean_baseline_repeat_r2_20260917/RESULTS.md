# Canonical clean-baseline repeatability — R2 closeout

Task: `docs/research/TASK_CANONICAL_CLEAN_BASELINE_REPEAT_R2_20260917.md`

Candidate presented to trusted host:
`4db0ef4d852be844814fd96d63198cbddaa5412b`

Trusted host record:
`docs/research/evidence/atlas_host/4db0ef4d852be844814fd96d63198cbddaa5412b.json`

Raw run:
`example/cpp/experiments/_runs/canonical_clean_baseline_repeatability_20260917/R2`

The `analysis.json` and `provenance.csv` beside this file are generated by the
analysis-only helper
`example/cpp/tools/analysis/analyze_canonical_clean_baseline_repeat_r2.py`.
No live process was launched during analysis. The trusted host record and raw
run are immutable inputs; this closeout contains Luna's proposed interpretation
only.

## FACTS

- The trusted record reports `launched=true`, return code `0`, `error=null`,
  `timed_out=false`, domain `220`, and manifest SHA256
  `f82f1a7dda8d41fb5e22cd235ade1b5ad4cbed73c91af74a338718c8938d8d01`.
  Its recorded host interval is `2026-09-17T14:48:03.593328+00:00` through
  `2026-09-17T14:49:58.418160+00:00`.
- The host index contains 18 raw artifacts. Every indexed size and SHA256 was
  independently recomputed and matched. The complete raw hash ledger is in
  `provenance.csv`; no raw file or trusted host record was changed.
- Host build provenance records candidate tree
  `7461d2f331b840dd6b06a330d8b92ca134929aea`, a clean tracked worktree,
  simulator SHA256
  `47c3ddfb996ded32fbb70e86bac0863df0888f6046339c5b446c2a0f159a4bf3`,
  controller SHA256
  `c01c87f03ba6fd32f533a29fcace5d0b1c139ff0c1ed97f8f2e657e645831f6d`, and
  scene SHA256
  `12286418247d0e240ae131b5ae5c60f3a7a481d4754aefe4517476e937aa05b8`.
  The MuJoCo header and library hashes, SDK hashes, and source identity are
  recorded in `analysis.json` and `provenance.csv`.
- DDS runtime facts are domain `220`, interface `lo`, CycloneDDS `0.10.2`,
  port base `4000`, maximum automatic participant index `31`, support artifact
  SHA256
  `0631e8aa1b9826215ef268665049d154571d3ca332eeff478b8fa9d17691293f`,
  and support-source SHA256
  `65100130add060af08f6404b03668f5018c9b039849bf62620cd99f362d38a3c`.
  Pre- and post-run snapshots contain empty Go2-process and known-shm sections.
- `simulator.log` contains the DDS-ready and shutdown markers. `controller.log`
  contains the 18-DoF model load, natural `LowState` settle, trot start,
  requested cycles 1 through 64, the pre-stop cycle-65 transition,
  `LOCOMOTION -> RETURN_TO_STAND`, `RETURN_TO_STAND -> LIE_DOWN`, and task
  completion. All recorded run status fields are zero.
- The first CSV sample is row 2 with `has_state=1`, `motion_stage=0`,
  `cycle_index=-1`, `cmd_time_s=0`, and `state_tick_s=1.728`; the first
  locomotion sample is row 2166 at cycle 0. The scientific attempt was
  therefore consumed at the first valid post-handoff state/control sample.
- The host-produced contact-ground-truth and dynamics artifacts both report
  `PASS`. The independent recomputation in `analysis.json` also reports
  `PASS` for both validations.

The frozen host command was:

```text
bash example/cpp/scripts/run_trot_exact_source.sh 90 _runs/canonical_clean_baseline_repeatability_20260917/R2 --controller-duration 70 --task stand-walk-lie --kernel raibert-trot --period 0.60 --duty 0.75 --step-length 0.091 --foot-lift 0.020 --kp 63 --kd 2.8 --raibert-velocity-gain 0.05 --raibert-max-adjustment 0.010 --world-feedback-max 0.060 --world-feedback-slew 0.004 --clean-baseline --tau-limit 35 --max-cycles 64 --headless --domain-id 220
```

## DERIVED METRICS

All calculations below are deterministic and are reproduced in `analysis.json`.

- `data.csv` contains 25,098 rows and 278 columns. Motion-stage counts are
  `0:1509`, `1:655`, `2:19630`, `3:1402`, `4:1500`, and `5:402`; every row has
  `has_state=1`. The requested-cycle window contains 19,229 samples.
- Nominal speed is `0.091 / 0.60 = 0.15166666666666667 m/s`.
  For `motion_stage == 2 and cycle_index >= 0`, the interval is
  `6.056 s` to `45.314 s`, with 19,630 samples and 5.682001836 m endpoint
  displacement. Ordinary least squares of `world_base_x_m` against
  `state_tick_s` gives `0.14625672682472507 m/s`; endpoint speed is
  `0.14473487788476233 m/s`, ratio `0.9643300669762093` of nominal.
- For requested cycles 1 through 64, the interval is `6.658 s` to `45.112 s`,
  with 19,229 samples. OLS speed is `0.14626717220732047 m/s`; endpoint speed
  is `0.1464293293545535 m/s`.
- Requested-cycle measured-velocity statistics are count `19,229`, minimum
  `0.105379555`, p05 `0.1140837632`, median `0.144530381`, mean
  `0.14638272354984658`, p95 `0.1827756178`, and maximum `0.198509591 m/s`.
- Controller health over cycles 1 through 64 gives maximum absolute roll
  `0.798946 deg`, pitch `0.64909 deg`, support drift `5.77282 mm`, joint error
  `0.107746 rad`, foot error `0.0347302 m`, and estimated torque `34.3155 Nm`.
  Minimum support is two contacts with support fraction `1.0`; maximum
  torque-over-limit samples and consecutive count are both zero.
- Requested-cycle CSV flags are success-valued throughout: footstep-plan
  valid, full SRBD WBC, full ID WBC, shadow solver/mapping/task/wrench/
  constraint checks are all `1`. Terrain is disabled and terrain failure or
  contact-rejection counts are zero. Controller-log counts for clean target
  infeasibility, strict WBC/QP rejection, cycle-quality guard, hard safety,
  and emergency stop are all zero.
- Ground-truth rows overlapping the controller stage-2 state-tick interval
  total 19,630. Their world-x velocity has mean `0.1447169448678 m/s`, median
  `0.142752757185 m/s`, p05 `0.1057260759489 m/s`, and p95
  `0.19612680072505 m/s`; maximum absolute roll and pitch are `2.52739149 deg`
  and `4.39185389 deg`.
- Contact validation covers 25,994 rows and 85,536 contact samples, with no
  negative vertical contact-GRF samples and maximum vertical GRF
  `429.144245762 N`. Dynamics validation covers 25,993 balance samples with
  p95 residual `0.628072635 N`, maximum residual norm `5.879063446 N`, and
  tolerance `10 N`.

The exact formulas, CSV windows, stage transitions, flag counts, host metadata,
raw hashes, source/binary hashes, and analyzer hash are machine-readable in
`analysis.json` and `provenance.csv`.

## LUNA INTERPRETATION

Luna's proposed classification is:

`CLEAN_BASELINE_FLAT_REPRODUCED`

All seven frozen gates evaluate true: valid handoff; 64/64 requested cycles;
normal lifecycle without hard or emergency stop; no clean target-feasibility
failure; no strict WBC/QP failure; OLS forward speed inside `[0.11, 0.19]`
m/s; and no gross tracking or stability failure.

The earliest supported boundary is scientific capture. The host established
DDS readiness and controller handoff, then completed the bounded flat-ground
canary. R2 is consumed; no retry, comparison run, tuning, or follow-up live
attempt is authorized by this closeout.

This is not an authoritative scientific judgment. Sol must independently
recompute the facts and derived metrics from the trusted record and immutable
raw files and may overturn Luna's interpretation without rerunning the canary.
This R2 result alone does not seal the three-replicate suite; the preregistered
R1 and R3 results remain required.
